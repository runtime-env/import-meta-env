name: CI

on:
  push:
  pull_request:

jobs:
  lts:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            node_version: 16
      fail-fast: false
    environment: HELLO
    name: "lts: ${{ matrix.os }}, node@${{ matrix.node_version }}, ${{ matrix.os }}"
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ^6

      - name: Set node version to ${{ matrix.node_version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node_version }}
          cache: "pnpm"

      - name: Install deps
        run: pnpm i

      - name: Test @import-meta-env/vite
        run: pnpm run test --parallel --stream --filter @import-meta-env/vite

      - name: Build @import-meta-env/vite
        run: pnpm run build --parallel --stream --filter @import-meta-env/vite

      - name: Install binary
        run: pnpm i

      - name: Build&Test examples
        run: |
          pnpm \
            --parallel \
            --stream \
            --filter \!html \
            --filter \!@import-meta-env/vite \
            --filter \!vue-ts \
            --filter \!vue-jsx \
            --filter \!env-var-defined-on-system \
            exec sh test.sh

      - name: Build&Test examples/vue-*
        if: ${{ matrix.node_version >= '14' }}
        run: |
          pnpm \
            --parallel \
            --stream \
            --filter vue-ts \
            --filter vue-jsx \
            exec sh test.sh

      - name: Build&Test examples/env-var-defined-on-system
        run: |
          pnpm i -g cross-env
          pnpm \
            --parallel \
            --stream \
            --filter env-var-defined-on-system \
            exec cross-env HELLO="${{ secrets.HELLO }}" sh test.sh

  compatibility:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        node_version: [12, 14, 16, 17]
        dotenv_version: [8, 9, 10, 11, 12, 13, 14, 15, 16]
        include:
          - os: macos-latest
            node_version: lts/*
            dotenv_version: latest
          - os: windows-latest
            node_version: lts/*
            dotenv_version: latest
    name: "compatibility: ${{ matrix.os }}, dotenv@${{ matrix.dotenv_version }}, node@${{ matrix.node_version }}"
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ^6

      - name: Set node version to ${{ matrix.node_version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node_version }}
          cache: "pnpm"

      - name: Install deps
        run: |
          pnpm i
          pnpm --parallel --stream --filter @import-meta-env/vite exec pnpm rm dotenv
          pnpm i -W -D dotenv@${{ matrix.dotenv_version }}

      - name: Test @import-meta-env/vite
        run: pnpm run test --parallel --stream --filter @import-meta-env/vite

      - name: Build @import-meta-env/vite
        run: pnpm run build --parallel --stream --filter @import-meta-env/vite

      - name: Install binary
        run: pnpm i

      - name: Test example
        run: |
          pnpm run test --parallel --stream --filter html

  lint:
    runs-on: ubuntu-latest
    name: "lint"
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 6

      - name: Set node version to 16
        uses: actions/setup-node@v2
        with:
          node-version: 16
          cache: "pnpm"

      - name: Install deps
        run: pnpm install

      - name: Check formatting
        run: pnpm prettier --check .
