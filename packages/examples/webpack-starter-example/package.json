{
  "private": true,
  "name": "webpack-starter-example",
  "scripts": {
    "dev:watch": "webpack --watch --mode development",
    "dev:start": "serve dist -p 8080",
    "dev": "rimraf bundle && cross-env HELLO=import-meta-env concurrently pnpm:dev:start pnpm:dev:watch",
    "build": "rimraf dist && webpack",
    "preview": "cross-env HELLO=import-meta-env import-meta-env -o dist/* && serve dist -p 4173",
    "test": "node test.js"
  },
  "devDependencies": {
    "@import-meta-env/cli": "workspace:*",
    "@import-meta-env/unplugin": "workspace:*",
    "concurrently": "7.0.0",
    "cross-env": "^7.0.3",
    "html-webpack-plugin": "^5.5.0",
    "rimraf": "^3.0.2",
    "serve": "^13.0.2",
    "webpack": "^5.69.1",
    "webpack-cli": "^4.9.2"
  }
}
