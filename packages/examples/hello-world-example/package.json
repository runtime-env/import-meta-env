{
  "private": true,
  "name": "hello-world-example",
  "scripts": {
    "build": "rimraf dist && webpack",
    "before-serve": "cross-env HELLO=world import-meta-env --example .env.example"
  },
  "devDependencies": {
    "@import-meta-env/cli": "workspace:*",
    "@import-meta-env/unplugin": "workspace:*",
    "cross-env": "^7.0.3",
    "rimraf": "^3.0.2",
    "webpack": "^5.69.1",
    "webpack-cli": "^4.9.2"
  }
}
