{
  "private": true,
  "name": "babel-starter-example",
  "scripts": {
    "dev": "rimraf dist && cross-env FOO=1 BAR=2 ./node_modules/.bin/babel src --out-dir dist",
    "build": "rimraf dist && cross-env NODE_ENV=production ./node_modules/.bin/babel src --out-dir dist",
    "populate": "cross-env FOO=1 BAR=2 final-env --example .env.example.public"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.6",
    "@babel/core": "^7.17.8",
    "@babel/preset-env": "^7.16.11",
    "@final-env/babel": "workspace:^0.1.6",
    "@final-env/cli": "workspace:*",
    "concurrently": "7.0.0",
    "cross-env": "^7.0.3",
    "rimraf": "^3.0.2"
  }
}
