{
  "private": true,
  "name": "rollup-starter-example",
  "scripts": {
    "dev:watch": "rollup -c -w",
    "dev:start": "pnpm -w run serve -d public -p 3000",
    "dev": "rimraf bundle && cross-env FOO=1 BAR=2 concurrently pnpm:dev:start pnpm:dev:watch",
    "build": "rimraf dist && pnpm build:pre && pnpm build:post",
    "build:pre": "rollup -c",
    "build:post": "cp public/index.html dist/index.html",
    "preview": "cross-env FOO=1 BAR=2 final-env --example .env.example.public && pnpm -w run serve -d dist -p 4173"
  },
  "devDependencies": {
    "@final-env/cli": "workspace:*",
    "@final-env/unplugin": "workspace:*",
    "@rollup/plugin-node-resolve": "^11.2.1",
    "concurrently": "7.0.0",
    "cross-env": "^7.0.3",
    "rimraf": "^3.0.2",
    "rollup": "^2.68.0"
  }
}
