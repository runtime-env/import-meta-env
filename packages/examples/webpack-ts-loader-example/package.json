{
  "private": true,
  "name": "webpack-ts-loader-example",
  "scripts": {
    "dev:watch": "webpack --watch",
    "dev:start": "pnpm -w run serve -d dist -p 3000",
    "dev": "rimraf bundle && cross-env HELLO=final-env NODE_ENV=development concurrently pnpm:dev:start pnpm:dev:watch",
    "build": "rimraf dist && cross-env NODE_ENV=production webpack",
    "preview": "cross-env HELLO=final-env final-env --example .env.example.public && pnpm -w run serve -d dist -p 4173"
  },
  "devDependencies": {
    "@final-env/cli": "workspace:*",
    "@final-env/unplugin": "workspace:*",
    "concurrently": "7.0.0",
    "cross-env": "^7.0.3",
    "html-webpack-plugin": "^5.5.0",
    "rimraf": "^3.0.2",
    "ts-loader": "^9.2.8",
    "typescript": "^4.6.2",
    "webpack": "^5.69.1",
    "webpack-cli": "^4.9.2"
  }
}
