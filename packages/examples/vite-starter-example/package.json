{
  "private": true,
  "name": "vite-starter-example",
  "scripts": {
    "dev": "cross-env FOO=1 BAR=2 vite",
    "build": "vite build",
    "preview": "cross-env FOO=1 BAR=2 final-env --example .env.example.public && vite preview"
  },
  "devDependencies": {
    "@final-env/cli": "workspace:*",
    "@final-env/unplugin": "workspace:*",
    "cross-env": "^7.0.3",
    "rimraf": "^3.0.2",
    "vite": "^2.8.4"
  }
}
