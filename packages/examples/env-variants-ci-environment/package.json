{
  "private": true,
  "name": "env-variants-ci-environment",
  "version": "0.0.0",
  "scripts": {
    "dev": "cross-env VITE_NAME=from-inline vite",
    "build": "cross-env VITE_NAME=from-inline vite build",
    "preview": "sh inject-env.sh && vite preview"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vite": "^2.7.13",
    "vite-plugin-dotenv": "workspace:^0.1.3"
  }
}
