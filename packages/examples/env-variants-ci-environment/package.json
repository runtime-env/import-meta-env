{
  "private": true,
  "name": "env-variants-ci-environment",
  "version": "0.0.0",
  "scripts": {
    "dev": "cross-env VITE_NAME=from-inline vite",
    "build": "vite build",
    "preview": "pnpm exec vite-plugin-dotenv && vite preview"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vite": "^2.7.13",
    "vite-plugin-dotenv": "workspace:^0.1.3"
  }
}
