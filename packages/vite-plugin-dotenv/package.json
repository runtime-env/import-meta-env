{
  "name": "vite-plugin-dotenv",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "author": "Ernest",
  "files": [
    "bin",
    "dist"
  ],
  "bin": {
    "dotenv": "bin/dotenv.sh"
  },
  "main": "dist/index.js",
  "types": "dist/plugin-vite/src/index.d.ts",
  "scripts": {
    "build": "rimraf dist && pnpm build-types && pnpm build-bundle",
    "build-bundle": "esbuild src/index.ts --bundle --platform=node --target=node12 --external:vite --external:dotenv --outfile=dist/index.js",
    "build-types": "tsc -p . --emitDeclarationOnly --outDir dist"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iendeavor/dotenv.git",
    "directory": "packages/plugin-vite"
  },
  "bugs": {
    "url": "https://github.com/iendeavor/dotenv/issues"
  },
  "homepage": "https://github.com/iendeavor/dotenv/tree/main/packages/plugin-vite#readme",
  "devDependencies": {
    "@types/node": "^17.0.8",
    "esbuild": "^0.14.11",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4",
    "vite": "^2.7.12"
  },
  "peerDependencies": {
    "vite": "^2.7.12"
  },
  "dependencies": {
    "@originjs/vite-plugin-federation": "^1.1.0",
    "chalk": "^5.0.0"
  }
}
